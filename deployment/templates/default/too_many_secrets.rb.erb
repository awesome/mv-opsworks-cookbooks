# Specify these secrets as ENV variables on your server,
# or save this file as config/too_many_secrets.rb

<% @secret.each do |variable, value| %>
ENV['<%= variable.to_s.upcase %>'] = "<%= value.to_s %>"
<% end %>
